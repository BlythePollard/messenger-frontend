import React from 'react'

const MessageForm = (props) => {
    return (
        <div>
            this is message form that will show once user is logged in
        </div>
    )
}

export default MessageForm;